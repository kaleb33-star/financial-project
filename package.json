{
  "name": "church-budget-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend + real database for the church budget dashboard (replaces localStorage).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
